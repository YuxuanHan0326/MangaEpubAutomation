<Page x:Class="MangaEpubAutomation.Gui.Views.Pages.LogsResultPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="LogsAndResult">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="2,0,0,10"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Text="Logs &amp; Result" />

        <ui:Card Grid.Row="1" Margin="0,0,0,10" Padding="14">
            <WrapPanel>
                <ui:Button Margin="0,0,10,8" Command="{Binding ClearLogsCommand}" Content="Clear Logs" />
                <ui:Button Margin="0,0,10,8" Command="{Binding LoadLatestPlanCommand}" Content="Load Latest Plan JSON" />
                <ui:Button Margin="0,0,10,8" Command="{Binding LoadLatestResultCommand}" Content="Load Latest Result JSON" />
                <TextBlock VerticalAlignment="Center" Text="{Binding LastResultPath}" />
            </WrapPanel>
        </ui:Card>

        <ui:Card Grid.Row="2" Padding="14">
            <TabControl>
                <TabItem Header="Live Log">
                    <TextBox Margin="0,8,0,0"
                             AcceptsReturn="True"
                             IsReadOnly="True"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             Text="{Binding LiveLogText}"
                             FontFamily="Consolas"
                             FontSize="12" />
                </TabItem>
                <TabItem Header="Plan JSON">
                    <TextBox Margin="0,8,0,0"
                             AcceptsReturn="True"
                             IsReadOnly="True"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             Text="{Binding PlanJsonPreview}"
                             FontFamily="Consolas"
                             FontSize="11" />
                </TabItem>
                <TabItem Header="Result JSON">
                    <TextBox Margin="0,8,0,0"
                             AcceptsReturn="True"
                             IsReadOnly="True"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             Text="{Binding ResultJsonPreview}"
                             FontFamily="Consolas"
                             FontSize="11" />
                </TabItem>
            </TabControl>
        </ui:Card>
    </Grid>
</Page>
